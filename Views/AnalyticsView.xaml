<UserControl x:Class="FinDesk.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"             xmlns:cvt="clr-namespace:FinDesk.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">
    <UserControl.Resources>
        <cvt:AmountToColorConverter x:Key="AmountToColorConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="📊 Аналітика" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Foreground="#2196F3" 
                   Margin="0,0,0,20"/>

        <!-- Періоди -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Період:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <ComboBox Width="200" 
                      ItemsSource="{Binding Periods}"
                      SelectedItem="{Binding SelectedPeriod}"
                      Padding="10,5"/>
            <Button Content="🔄 Оновити" 
                    Command="{Binding RefreshCommand}" 
                    Margin="20,0,0,0"
                    Padding="15,8"
                    Background="#2196F3"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
            <Button Content="📄 Експорт" 
                    Command="{Binding ExportCommand}" 
                    Margin="10,0,0,0"
                    Padding="15,8"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>

        <!-- Основний контент -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Статистичні картки -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Прибутки -->
                    <Border Grid.Column="0" Background="#E8F5E9" Padding="20" Margin="0,0,10,0" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="📈 Прибутки" FontSize="14" Foreground="#4CAF50" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding TotalIncome, StringFormat='{}{0:N2} ₴'}" 
                                       FontSize="24" 
                                       FontWeight="Bold" 
                                       Foreground="#2E7D32" 
                                       Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Витрати -->
                    <Border Grid.Column="1" Background="#FFEBEE" Padding="20" Margin="0,0,10,0" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="📉 Витрати" FontSize="14" Foreground="#F44336" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding TotalExpenses, StringFormat='{}{0:N2} ₴'}" 
                                       FontSize="24" 
                                       FontWeight="Bold" 
                                       Foreground="#C62828" 
                                       Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Cash Flow -->
                    <Border Grid.Column="2" Background="#E3F2FD" Padding="20" Margin="0,0,10,0" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="💰 Cash Flow" FontSize="14" Foreground="#2196F3" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding NetCashFlow, StringFormat='{}{0:N2} ₴'}" 
                                       FontSize="24" 
                                       FontWeight="Bold" 
                                       Foreground="#1976D2" 
                                       Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Середньодобове -->
                    <Border Grid.Column="3" Background="#FFF3E0" Padding="20" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="📅 Середнє/день" FontSize="14" Foreground="#FF9800" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding AverageDaily, StringFormat='{}{0:N2} ₴'}" 
                                       FontSize="24" 
                                       FontWeight="Bold" 
                                       Foreground="#F57C00" 
                                       Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Топ категорій -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#FAFAFA" Padding="20" CornerRadius="8" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="📈 Місячний тренд" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,15"/>
                            <lvc:CartesianChart Series="{Binding TrendSeries}" 
                                                XAxes="{Binding TrendAxes}"
                                                LegendPosition="Top"
                                                Height="250"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#FAFAFA" Padding="20" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="💳 Дохід vs Витрати" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,15"/>
                            <lvc:CartesianChart Series="{Binding DebitCreditSeries}"
                                                XAxes="{Binding DebitCreditAxes}"
                                                Height="250"
                                                LegendPosition="Top"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Background="#FAFAFA" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="🏆 Топ-10 Категорій" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <ListView Grid.Column="0" ItemsSource="{Binding TopCategories}" 
                                      BorderThickness="0"
                                      Background="Transparent">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" 
                                            Padding="15,10" 
                                            Margin="0,5" 
                                            CornerRadius="6"
                                            BorderBrush="#E0E0E0"
                                            BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <TextBlock Grid.Column="0" 
                                                       Text="{Binding Name}" 
                                                       FontWeight="SemiBold" 
                                                       VerticalAlignment="Center"/>
                                                
                                                <TextBlock Grid.Column="1" 
                                                       Text="{Binding Count, StringFormat='{}{0} транз.'}" 
                                                       Foreground="#757575" 
                                                       Margin="0,0,20,0"
                                                       VerticalAlignment="Center"/>
                                                
                                                <TextBlock Grid.Column="2" 
                                                       Text="{Binding Amount, StringFormat='{}{0:N2} ₴'}" 
                                                       FontWeight="Bold" 
                                                       FontSize="16"
                                                       VerticalAlignment="Center">
                                                    <TextBlock.Foreground>
                                                        <SolidColorBrush Color="{Binding Amount, Converter={StaticResource AmountToColorConverter}}"/>
                                                    </TextBlock.Foreground>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <lvc:PieChart Grid.Column="1"
                                          Series="{Binding CategorySeries}"
                                          LegendPosition="Bottom"
                                          Height="260"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
